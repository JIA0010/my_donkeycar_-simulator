# Donkey Carシミュレーター環境構築 - トラブルシューティング全記録

## 📋 概要

このドキュメントは、Donkey Carシミュレーター環境をmacOS（Apple Silicon/ARM64）で構築する際に遭遇したすべての技術的問題と解決策を記録したものです。

**実施日**: 2026年1月1日  
**OS**: macOS (Apple Silicon)  
**Python**: 3.13  
**プロジェクトパス**: `/Users/yoshimurahiro/mysim`

---

## 🔴 問題1: pkg_resources モジュールが見つからない

### エラーメッセージ
```
ModuleNotFoundError: No module named 'pkg_resources'
```

### 発生タイミング
`donkey createcar --path ./mycar` コマンド実行時

### 原因
- Python 3.13では `setuptools` がデフォルトでインストールされていない
- Donkey Carライブラリが `pkg_resources`（setuptoolsの一部）に依存している

### 解決方法
```bash
source env/bin/activate
pip install setuptools
```

### 技術的詳細
- `pkg_resources` は setuptools パッケージの一部
- Python 3.12以降、setuptoolsがデフォルトから外された
- 多くのレガシーパッケージがこの問題に直面している

### 予防策
requirements.txtに明示的に記載：
```
setuptools>=80.0.0
```

---

## 🔴 問題2: collections.MutableMapping の AttributeError

### エラーメッセージ
```
AttributeError: module 'collections' has no attribute 'MutableMapping'
```

### 発生タイミング
`donkey createcar` コマンド実行時

### 原因
- 古いバージョンの Tornado (4.5.3) がインストールされていた
- Python 3.10以降、`collections.MutableMapping` は `collections.abc.MutableMapping` に移動
- 古いコードが新しいPythonと互換性がない

### 解決方法
```bash
pip install --upgrade tornado
```

### 技術的詳細
- Python 3.3で `collections.abc` が導入
- Python 3.10で `collections` からの直接インポートが廃止
- Tornado 4.5.3は2017年リリースで、この変更に対応していない
- Tornado 6.x系で修正済み

### バージョン詳細
- ❌ Tornado 4.5.3 (失敗)
- ✅ Tornado 6.5.4 (成功)

---

## 🔴 問題3: myconfig.py のシンタックスエラー

### エラーメッセージ
```
SyntaxError: unterminated string literal (detected at line 7)
```

### 発生タイミング
`python manage.py drive` コマンド実行時

### 原因
- ファイル作成時に何らかの理由でファイルが破損
- 複数回の編集で内容が重複・混在
- VSCodeのファイル書き込みとターミナルの競合の可能性

### 発生したファイル内容（破損例）
```python
""" """ # """ 

My CAR CONFIG 

My CAR CONFIG # My CAR CONFIG 

This file is read by your car application's manage.py script to change the car

performance. This configuration is set up for simulator mode.
```

### 解決方法
ターミナルから直接ファイルを作成：
```bash
rm -f /path/to/myconfig.py
cat > /path/to/myconfig.py << 'EOF'
"""
My CAR CONFIG
...
"""
EOF
```

### 技術的詳細
- `create_file` ツールで作成したファイルが予期せず破損
- 複数の編集ツールでの同時操作が原因の可能性
- heredoc (`<< 'EOF'`) を使用することで確実に作成

### 予防策
1. 重要な設定ファイルはバックアップを作成
2. テンプレートファイルを用意
3. ファイル作成後、必ず構文チェック：
   ```bash
   python -m py_compile myconfig.py
   ```

---

## 🔴 問題4: gym モジュールが見つからない

### エラーメッセージ
```
ModuleNotFoundError: No module named 'gym'
```

### 発生タイミング
`python manage.py drive` コマンド実行時（シミュレーターモード）

### 原因
- Donkey Car本体をインストール時に `[pc]` オプションを指定していなかった
- または、gym-donkeycar パッケージがインストールされていない

### 解決方法
```bash
pip install gym-donkeycar
```

### 技術的詳細
- Donkey Carは実機用とシミュレーター用で依存関係が異なる
- `donkeycar[pc]` オプションで追加パッケージをインストールすべき
- gym-donkeycarは OpenAI Gym のDonkey Car環境実装

### 正しいインストール手順
```bash
# 方法1: pc オプション付き
pip install "donkeycar[pc]"

# 方法2: 個別インストール
pip install donkeycar
pip install gym-donkeycar
```

---

## 🔴 問題5: Donkey環境が登録されていない

### エラーメッセージ
```
gym.error.NameNotFound: Environment donkey-generated-track doesn't exist.
```

### 発生タイミング
シミュレーターとの接続開始時

### 原因
- PyPI版の gym-donkeycar (1.0.13) が古く、環境が正しく登録されない
- バージョン不一致により環境名の認識に問題

### 解決方法
GitHubから最新版をインストール：
```bash
pip uninstall -y gym gym-donkeycar
pip install git+https://github.com/tawnkramer/gym-donkeycar
```

### 技術的詳細
- PyPI版 (1.0.13) は2021年リリース
- GitHub版 (1.3.1) は2023年以降更新
- Donkey Car v5.0.0との互換性は最新版のみ

### バージョン比較
| 場所 | バージョン | 状態 |
|-----|----------|------|
| PyPI | 1.0.13 | ❌ 古い |
| GitHub (最新) | 1.3.1 | ✅ 推奨 |

---

## 🔴 問題6: Gym廃止警告とNumPy 2.0非互換

### 警告メッセージ
```
Gym has been unmaintained since 2022 and does not support NumPy 2.0 amongst other critical functionality.
Please upgrade to Gymnasium, the maintained drop-in replacement of Gym
```

### 発生タイミング
シミュレーター起動時

### 原因
- OpenAI Gymが2022年に開発終了
- NumPy 2.0との互換性なし
- Gymnasium への移行が推奨されている

### 現在の状態
⚠️ **警告のみで動作は可能**

### 将来的な解決方法
```bash
# Gymnasium への移行（将来的に必要になる可能性）
pip uninstall gym
pip install gymnasium
pip install gym-donkeycar  # Gymnasium対応版
```

### 技術的詳細
- Gym は Farama Foundation に引き継がれ Gymnasium として継続
- gym-donkeycar は現在まだ Gymnasium に完全移行していない
- 当面は Gym 0.22 を使用することで回避可能

### 互換性マトリクス
| パッケージ | バージョン | NumPy 2.0 | 状態 |
|-----------|----------|-----------|------|
| gym | 0.22 | ❌ | 使用中（警告あり） |
| gym | 0.26 | ❌ | 非推奨 |
| gymnasium | 1.2.3 | ✅ | 将来推奨 |

---

## 🔴 問題7: システムPythonへの誤インストール

### 問題の内容
```
Defaulting to user installation because normal site-packages is not writeable
```

### 発生タイミング
`pip install` コマンド実行時（仮想環境を有効化せずに実行）

### 原因
- 仮想環境を有効化せずに pip コマンドを実行
- システムのPython 3.12 のユーザーディレクトリに誤ってインストール
- 環境の汚染とバージョン競合の原因に

### 影響
- システムPythonが汚染される
- プロジェクト間でパッケージが競合
- 削除が面倒

### 解決方法
1. システムからパッケージを削除：
```bash
python3.12 -m pip uninstall -y donkeycar gym gym_donkeycar gym-notices gymnasium farama-notifications cloudpickle
```

2. 仮想環境のpipを使用：
```bash
/Users/yoshimurahiro/mysim/env/bin/pip install <package>
```

### 予防策
スクリプトでフルパス指定を使用：
```bash
#!/bin/bash
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
"${SCRIPT_DIR}/env/bin/python" manage.py drive
```

### 技術的詳細
- `--user` フラグが自動的に付与される
- `~/.local/lib/python3.12/site-packages` にインストールされる
- 仮想環境よりもシステムのパスが優先される場合がある

---

## 🔴 問題8: 仮想環境の有効化失敗

### エラーメッセージ
```
zsh: command not found: python
source: no such file or directory: ../env/bin/activate
```

### 発生タイミング
スクリプト内で `source env/bin/activate` を使用した場合

### 原因
1. カレントディレクトリが想定と異なる
2. `source` コマンドが相対パスで失敗
3. zshの動作がbashと異なる

### 解決方法
スクリプトでフルパスを使用：
```bash
#!/bin/bash
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "${SCRIPT_DIR}"
source "${SCRIPT_DIR}/env/bin/activate"
```

または、直接Pythonをフルパスで指定：
```bash
"${SCRIPT_DIR}/env/bin/python" manage.py drive
```

### 技術的詳細
- `source` は現在のシェルで実行される
- サブシェルでは環境変数が引き継がれない
- フルパス指定が最も確実

### 推奨アプローチ
**フルパス指定を使用し、`source activate` に依存しない**

---

## 🔴 問題9: Python 3.13との互換性問題

### 問題の内容
Python 3.13は比較的新しく、一部のパッケージが対応していない

### 影響を受けたパッケージ
1. setuptools（デフォルトで含まれない）
2. 古いバージョンのTornado
3. 一部のC拡張モジュール

### 解決方法
個別に対応：
```bash
pip install setuptools  # 明示的にインストール
pip install --upgrade tornado  # 最新版に更新
```

### 推奨環境
**Python 3.9 - 3.11を推奨**

理由：
- Donkey Car公式の推奨範囲
- TensorFlowとの互換性
- 安定性

### Python 3.13を使用する場合の注意点
1. setuptoolsを明示的にインストール
2. すべてのパッケージを最新版に更新
3. 互換性問題が発生する可能性を認識

---

## 🔴 問題10: シミュレーターの「Play!」ボタンに関する混乱

### 問題の内容
ドキュメントに「Play!ボタンを押す」と記載されているが、新しいバージョンでは表示されない

### 原因
- シミュレーターのバージョンによってUI/UXが異なる
- 古いバージョン: Unity起動画面に「Play!」ボタン
- 新しいバージョン: 設定完了後自動的に起動

### 解決方法
ドキュメントを更新：

**旧版（誤解を招く）**:
```
「Play!」ボタンをクリック
```

**新版（正確）**:
```
設定画面で：
1. Scene: Generated Track を選択
2. Graphics: Good または Fast を選択
3. 設定が完了すると自動的にシミュレーターが起動
   （「Play!」ボタンはバージョンによって表示される場合とされない場合があります）
```

### 技術的詳細
- Unity起動設定画面のバージョンによる違い
- macOS版、Windows版、Linux版で挙動が異なる場合がある
- シミュレーター接続は設定画面を経由せず直接起動する場合もある

---

## 📊 問題の難易度と頻度

| 問題 | 難易度 | 頻度 | 影響度 |
|-----|--------|------|--------|
| pkg_resources不足 | ⭐⭐ | 高 | 中 |
| collections.MutableMapping | ⭐⭐⭐ | 中 | 高 |
| myconfig.py破損 | ⭐⭐⭐⭐ | 低 | 高 |
| gymモジュール不足 | ⭐⭐ | 高 | 高 |
| Donkey環境未登録 | ⭐⭐⭐ | 高 | 高 |
| Gym廃止警告 | ⭐ | 高 | 低 |
| システム汚染 | ⭐⭐⭐⭐ | 中 | 中 |
| 仮想環境有効化失敗 | ⭐⭐ | 高 | 中 |
| Python 3.13互換性 | ⭐⭐⭐ | 中 | 中 |
| UI/UX混乱 | ⭐ | 低 | 低 |

**難易度**: ⭐（簡単） 〜 ⭐⭐⭐⭐⭐（非常に難しい）

---

## 🎯 根本原因の分析

### 1. バージョン互換性の問題
- **原因**: Python 3.13が新しすぎる
- **対策**: Python 3.9-3.11の使用を推奨

### 2. パッケージの更新頻度の違い
- **原因**: PyPIとGitHubでバージョンが異なる
- **対策**: GitHubから直接インストール

### 3. ドキュメントの古さ
- **原因**: 公式ドキュメントが更新されていない
- **対策**: 実際の動作に基づいてドキュメント作成

### 4. 環境管理の複雑さ
- **原因**: 仮想環境の使い方が徹底されていない
- **対策**: スクリプトでフルパス指定

---

## 💡 ベストプラクティス（学んだこと）

### 1. 環境構築
```bash
# Python バージョンを確認
python3 --version  # 3.9-3.11を推奨

# 仮想環境を作成
python3 -m venv env

# 仮想環境のpipを直接使用（source activateに依存しない）
./env/bin/pip install --upgrade pip
./env/bin/pip install donkeycar
./env/bin/pip install git+https://github.com/tawnkramer/gym-donkeycar
./env/bin/pip install setuptools
```

### 2. 設定ファイル管理
```bash
# 設定ファイルのバックアップ
cp myconfig.py myconfig.py.backup

# 構文チェック
python -m py_compile myconfig.py
```

### 3. スクリプト作成
```bash
#!/bin/bash
# スクリプトのディレクトリを取得
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# フルパスで仮想環境のPythonを使用
"${SCRIPT_DIR}/env/bin/python" manage.py drive
```

### 4. 依存関係の記録
```bash
# requirements.txt を作成
./env/bin/pip freeze > requirements.txt

# 環境の再現
./env/bin/pip install -r requirements.txt
```

---

## 🔧 トラブルシューティングチェックリスト

問題が発生した場合、以下を順番に確認：

### ステップ1: Python環境の確認
```bash
# Pythonバージョン
python3 --version

# 仮想環境の存在確認
ls -la env/

# 仮想環境内のパッケージ
./env/bin/pip list | grep -E "donkey|gym"
```

### ステップ2: システムの汚染チェック
```bash
# システムPythonのパッケージ確認
python3 -m pip list --user | grep -E "donkey|gym"

# 汚染されている場合は削除
python3 -m pip uninstall -y donkeycar gym gym_donkeycar
```

### ステップ3: 設定ファイルの確認
```bash
# 構文チェック
python3 -m py_compile mycar/myconfig.py

# 設定内容の確認
cat mycar/myconfig.py | grep -E "DONKEY_GYM|SIM_HOST"
```

### ステップ4: シミュレーター接続の確認
```bash
# ポートの確認
lsof -i :9091
lsof -i :8887

# ネットワーク接続の確認
netstat -an | grep 9091
```

### ステップ5: システム状態の確認
```bash
# 自動チェックスクリプト
./check_system.sh
```

---

## 📝 タイムライン（問題発生順）

1. **pkg_resources不足** → setuptoolsインストール
2. **collections.MutableMapping** → Tornadoアップグレード
3. **myconfig.py破損** → heredocで再作成
4. **gymモジュール不足** → gym-donkeycarインストール
5. **Donkey環境未登録** → GitHubから最新版インストール
6. **システムPython汚染** → クリーンアップ実施
7. **スクリプト修正** → フルパス指定に変更
8. **ドキュメント整備** → トラブルシューティングガイド作成

---

## 🎓 技術的な学び

### Python仮想環境について
- **source activate は万能ではない**
  - スクリプトでは失敗しやすい
  - フルパス指定が確実

- **システムを汚染しない**
  - 必ず仮想環境のpipを使用
  - `--user` フラグに注意

### パッケージ管理について
- **PyPIが最新とは限らない**
  - GitHubの方が新しい場合がある
  - メンテナンスされているか確認

- **依存関係を明示する**
  - requirements.txt は必須
  - バージョンを固定すべき場合もある

### Python バージョンについて
- **最新 ≠ 最適**
  - Python 3.13は新しすぎる
  - 安定版を選ぶべき

---

## 🚀 推奨セットアップ手順（完全版）

```bash
# 1. プロジェクトディレクトリ作成
mkdir -p ~/mysim
cd ~/mysim

# 2. 仮想環境作成（Python 3.9-3.11推奨）
python3.11 -m venv env

# 3. pipアップグレード
./env/bin/pip install --upgrade pip

# 4. 必須パッケージインストール
./env/bin/pip install setuptools
./env/bin/pip install donkeycar
./env/bin/pip install git+https://github.com/tawnkramer/gym-donkeycar

# 5. mycar作成
./env/bin/donkey createcar --path ./mycar

# 6. myconfig.pyを編集（シミュレーター設定）
# DONKEY_GYM = True を設定

# 7. requirements.txt作成
./env/bin/pip freeze > requirements.txt

# 8. 動作確認
./env/bin/python mycar/manage.py drive
```

---

## 📚 参考資料

### 公式ドキュメント
- [Donkey Car公式](https://docs.donkeycar.com/)
- [gym-donkeycar GitHub](https://github.com/tawnkramer/gym-donkeycar)

### Python関連
- [Python仮想環境ガイド](https://docs.python.org/ja/3/library/venv.html)
- [pip ユーザーガイド](https://pip.pypa.io/en/stable/user_guide/)

### トラブルシューティング
- Python 3.10+ での collections 変更: [PEP 585](https://peps.python.org/pep-0585/)
- Gym → Gymnasium 移行: [Migration Guide](https://gymnasium.farama.org/introduction/migration_guide/)

---

**📅 最終更新**: 2026年1月1日  
**📝 作成者**: セットアップ実施者  
**🔖 バージョン**: 1.0

このドキュメントは実際の環境構築で発生した問題をすべて記録しています。
同じ問題に直面した際の参考にしてください。
